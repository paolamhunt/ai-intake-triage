# Runtime mode: development, test, or production
AIT_ENVIRONMENT=development

# Replace with a unique random value containing at least 32 characters.
AIT_OPERATOR_API_KEY=replace-with-a-secure-random-api-key

# Trusted YAML configuration loaded when the application starts
AIT_BUSINESS_CONFIG_PATH=configs/the_distracted_developer.yaml